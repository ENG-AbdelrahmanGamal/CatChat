<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/indexFragment">

    <fragment
        android:id="@+id/indexFragment"
        android:name="com.example.catchat.IndexFragment"
        android:label="@string/index"
        tools:layout="@layout/fragment_index" >
    </fragment>
    <fragment
        android:id="@+id/helpFragment"
        android:name="com.example.catchat.HelpFragment"
        android:label="@string/help"
        tools:layout="@layout/fragment_help" />
    <fragment
        android:id="@+id/sentFragment2"
        android:name="com.example.catchat.SentFragment"
        android:label="@string/sent"
        tools:layout="@layout/fragment_sent" >
    </fragment>
</navigation>